{
  "name": "ohm",
  "version": "0.1.0",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ ohm",
    "create-local": "graph create --node http://localhost:8020/ ohm",
    "remove-local": "graph remove --node http://localhost:8020/ ohm",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 ohm"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
